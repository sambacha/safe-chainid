{
  "name": "safe_chainid",
  "version": "0.0.1",
  "description": "safely handle chainIds",
  "main": "source/index.ts",
  "scripts": {
    "test": "jest",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "keywords": [
    "ethereum",
    "chain id",
    "blockchain",
    "metamask",
    "chainid",
    "networkid"
  ],
  "author": "sam bacha <noreply@github.com>",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "ts-jest": "^27.0.5",
    "typescript": "^4.0.3",
    "web3": "^1.5.2"
  }
}
